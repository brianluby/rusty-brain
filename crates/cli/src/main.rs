//! CLI scripts (find, ask, stats, timeline) for rusty-brain.

fn main() {
    println!("rusty-brain cli");
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
